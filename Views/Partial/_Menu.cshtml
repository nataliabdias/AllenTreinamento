<!-- Fixed navbar -->
<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">Exemplo Mvc</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li @if (ViewBag.Menu == "HOME")
                    {<text> class="active"</text>}><a href="/Home">Home</a></li>

                @if (Roles.IsUserInRole("Administrador"))
                {    
                    <li @if (ViewBag.Menu == "USER")
                        {<text> class="active"</text>}><a href="/User">Usuario</a></li>
                }
                @if (Roles.IsUserInRole("Administrador"))
                {    
                    <li @if (ViewBag.Menu == "PESSOA")
                        {<text> class="active"</text>}><a href="/Pessoa">Pessoa</a></li>
                }
            </ul>
            @{
                Html.RenderAction("RenderMenu", "Home");  
            }
        </div>



        <!--/.nav-collapse -->
    </div>
</nav>

